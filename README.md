# diamondAI

Flask + Ollama + VRM 으로 만든 아바타 챗봇. 이름은 **다이아**.

말을 하고, 얼굴이 바뀌고, 몸이 움직이고, 만지면 반응한다.
사람과의 사이가 대화에 따라 오르내리고, 그 사이가 말투와 반응을 정한다.

---
실행하기 https://dia.kdt2025.com/login   

<img width="1916" height="919" alt="image" src="https://github.com/user-attachments/assets/28bd4201-a0c3-4dc8-b7f7-fa322521f59c" />

## 돌리는 법

```bash
pip install flask requests
python main.py
```

`http://127.0.0.1:5000`

### 따로 넣어야 하는 것

| 파일 | 무엇 |
|---|---|
| `static/avatar.vrm` | 옷 입은 아바타 |
| `static/표현용.vrm` | 맨몸 + 절정 표정용 |

두 파일은 라이선스가 `Redistribution_Prohibited` 라 저장소에 없다.
표현용이 없으면 `avatar.py` 의 `model["layered"]` 를 `False` 로 둔다.

`config.py` 의 `OLLAMA_URL` 과 `OLLAMA_MODEL` 을 쓰는 서버에 맞춘다.

### 밖에 올리기 (Vercel)

```bash
npx vercel login
npx vercel deploy --prod
```

올린 데서는 **파일을 쓸 수 없다.** `store.py` 가 그것을 가려 준다 —
`BLOB_READ_WRITE_TOKEN` 이 있으면 Vercel Blob 에, 없으면 지금까지처럼 파일에 적는다.
저장소는 `npx vercel blob create-store <이름> --access private` 로 만든다.
**private 이어야 한다** — 안에 비밀번호 해시와 나눈 이야기가 들어간다.

넣어야 하는 환경변수:

| 이름 | 없으면 |
|---|---|
| `SECRET_KEY` | 기계가 바뀔 때마다 모두 로그아웃된다 (`python -c "import secrets;print(secrets.token_hex(32))"`) |
| `SIGNUP_CODE` | 주소를 아는 사람은 누구나 계정을 만든다 |
| `VRM_URL` | **아바타가 안 나온다.** 파일이 `Redistribution_Prohibited` 라 짐에 못 싣는다 |

#### 모델 서버에 닿게 하기

올린 사이트는 모델 서버(`cju.nezip.co.kr:11434`)에 **닿지 못한다.**
미국도 서울도 연결 자체가 안 된다 — 바깥으로 나가는 것은 되므로
(목소리는 1.3초에 온다) 그 서버·그 포트만 막힌 것이다.

내 컴퓨터가 가운데 서서 이어 준다.

```bash
python _tunnel.py
```

cloudflared 가 https 주소를 하나 만들고 그리로 온 것을 모델 서버로 넘긴다.
443 으로 나가므로 안 막힌다. **주소는 띄울 때마다 바뀌지만** 저장소에 적어 두고
사이트가 부를 때마다 읽으므로 **다시 배포할 필요는 없다.**

이 창을 켜 둔 동안만 올린 사이트가 답한다.

### 처음 들어갈 때

계정이 없으면 회원가입 칸이 먼저 뜬다. **기억은 계정마다 따로다** —
회원가입은 빈 기억으로 시작하고, 로그인은 그 사람이 쌓아 둔 기억을 연다.

계정을 나누기 전부터 쓰던 `memory_store.json` 이 있다면 **맨 처음 만드는
계정 하나가** 그것을 물려받는다(보관함까지). 두 번째 계정부터는 빈 기억이다.
무엇을 물려받는지는 회원가입 화면에 적힌다.

`accounts.json`(계정) · `.secret_key`(쿠키 서명) · `memory/`(사람마다의 기억)
는 저장소에 올라가지 않는다.

---

## 화면

| 주소 | 무엇 |
|---|---|
| `/` | 대화 화면 |
| `/model-test` | 표정·동작·옷 시험대 |
| `/rig` | 뼈 각도 확인대. 손 모양까지 |
| `/face` | 표정 배합기. 그룹·조각을 섞어 이름을 붙인다(저장은 이 컴퓨터에서만) |
| `/pixel` | 픽셀창 |

---

## 짜임새

**하나의 개체가 전부 소유한다.** `avatar.py` 의 `VirtualAvatar` 가
페르소나·표정·동작·관계·만지기·행동을 모두 들고 있고, 화면은
`/api/avatar` 로 그 정의를 받아 쓴다.

원래는 페르소나가 `ai_brain.py` 에, 표정 수치가 `index.html` 에 따로
적혀 있어서 한쪽만 고치면 어긋났다. 그걸 없애려는 구조다.

```
avatar.py          개체. 이 프로젝트의 중심
ai_brain.py        대화 엔진. 표시 추출, 관계 갱신, 고백 판정
memory_manager.py  기억 저장 (memory_store.json)
main.py            Flask 경로
config.py          모델·목소리 설정
templates/         화면
_verify_*.py       검사 스크립트
```

---

## 무엇이 되는가

### 표정
9종 — 평온·기쁨·즐거움·슬픔·화남·놀람·눈 감기·윙크 둘.
수치는 아바타 파일(VRoid 에서 내보낸 그대로)의 표정 그룹 값이다(전부 100).
옛 아바타에서 조각을 섞어 만든 얼굴 22개는 2026-09-17 에 지웠다.
새 얼굴은 표정 배합기(`/face`)에서 만든다.

표정마다 **언제 짓는 얼굴인지**(`when`)가 적혀 있고, 그 글은 모델에게도
그대로 건네진다. 그래야 낱말이 안 걸린 문장에서도 스스로 고른다.

모델은 이모지로도, `(표정: 이름)` 처럼 이름으로도 얼굴을 고를 수 있다.

배합기에서 만들거나 덮은 것은 `expressions_custom.json` 에 적힌다.
지우면 아바타 파일의 값으로 돌아간다.

### 동작
13종. 손인사·걷기·고개 젓기·끄덕임·가위바위보 세 손·팔짱·얼굴 가리기·
기지개·등 돌리기·쑥스러워하기.

키프레임은 `_fit_*.py` 가 역기구학으로 풀어 만들었고,
`_verify_collision.py` 가 몸을 뚫지 않는지 20ms 간격으로 검사한다.

**표정과 몸짓의 규칙은 두 줄이다.**
1. 자리가 정한 표정이 이긴다. 동작은 얼굴이 비어 있을 때만 데려온다.
2. 얼굴이 곧 그 동작인 몸짓(쑥스러워하기·얼굴 가리기)만 예외다.

### 관계
10단계. 침묵 → 원수 → 냉랭함 → 서먹함 → 친구 → 가까운 사이 →
사랑 → 집착 → 광기 → 얀데레.

- **바닥(침묵)에서는 모델을 부르지 않는다.** 부르면 무슨 말이든 하게 되어
  '대답하지 않는다' 가 '차갑게 대답한다' 로 변한다
- **광기로 넘어가려면 고백을 주고받아야 한다.** 연인이 아니면 호감이
  229 에서 멈춘다
- **얀데레는 되돌아가지 않는다.** 부정적인 표현이 사라지고, 잠들지 않고,
  대답이 없어도 혼자 말을 잇는다
- **순종** — 호감 상한(660)을 넘어 흘러넘친 점수를 100점당 1 로 모은다.
  50 이 되면 무엇이든 따른다. 얀데레는 유지된다

### 기분
친밀도와 **다른** 0~6칸 상태. 사이가 좋아도 방금 심한 말을 들었으면
상해 있을 수 있다. 쓰다듬으면 풀리고, 3분마다 한 칸씩 저절로 풀린다.

### 만지기
자리 12곳 x 도구 5가지. 판정은 메시 레이캐스트가 아니라 **본에 붙는
보이지 않는 공 63개** 로 한다. 굵기는 그 본이 끌고 다니는 살에서
실제로 재서 만들었다(`_gen_hitboxes.py`).

- 자리마다 허락되는 친밀도가 다르다
- 사이가 깊어지면 같은 자리라도 얼굴과 몸짓이 달라진다
- 옷은 잡아당길 수 있고, 두 번 누르면 벗는다
- 괄호로 쓴 행동(`(머리를 쓰다듬는다)`)도 같은 표를 탄다

### 그 밖
- **목소리** — 브라우저 목소리 기본. `config.py` 에 키를 넣으면
  Gemini TTS(아케르나르)로 바뀐다. 입 모양은 실제 소리 길이에 맞춘다
- **배경** — `static/background/` 에 이미지를 넣으면 깔린다
- **가위바위보** — 손가락 관절로 실제 손 모양을 짓는다
- **먼저 말 걸기 · 잠들기 · 돌아다니기**

---

## 명령어

| | |
|---|---|
| `/도움말` | 전체 목록 |
| `/표정 <이름>` `/<동작이름>` | 얼굴과 몸짓 |
| `/호감 초기화` `/호감 <숫자>` | 사이만 되돌리기 (기억은 그대로) |
| `/새 기억` `/리셋` | 기억을 보관하고 처음부터 / 되찾기 |
| `/배경` `/목소리` `/옷` | 배경 넘기기 · 소리 켜기 · 옷 끌기 진단 |

---

## 검사

```bash
python _verify_collision.py     # 동작이 몸을 뚫지 않는지
python _verify_touch.py         # 만지는 자리와 도구가 다 채워졌는지
python _verify_expressions.py   # 표정 그룹·조각 이름이 아바타 파일에 있는지
python _verify_pairs.py         # 한 동작에 표정이 하나만 걸리는지
python _verify_stages.py        # 관계 단계의 말투가 어긋나지 않는지
```


---

## 버전

새 판을 낼 때는 **아래 표에 한 줄 적고** 태그를 붙인다.

```bash
git tag -a v1.0 -m "무엇이 달라졌는지"
git push origin v1.0
```

세 번째 자리(v0.6.1)는 **고침만 있고 새로 생긴 것은 없을 때** 쓴다.

| 판 | 날짜 | 무엇이 달라졌나 |
|---|---|---|
| **v1.6** | 2026-09-15 | **놀이 다섯과 장소.** 체스(python-chess) · 끝말잇기(`word_chain.py`) · 오목(`gomoku.py`, 다섯 칸 창을 세어 값을 매김) · 할리갈리(`halli.py`, 세기는 두는 눈이 아니라 **반응 시간** 0.8~1.5초) · 장기(`janggi.py`, 라이브러리가 없어 포·궁성·마상 막힘·빅장·외통을 직접 — 한 수 1.39초). 전부 같은 자리 — **규칙과 둘 자리는 서버가 쥐고 화면은 판을 그리고 누른 자리만 보낸다**, 낱말·수 고르기에 모델은 안 부른다(0.0초) · **장소** — 배경 파일 이름이 곧 장소 이름(`공원.jpg`→'공원'), 산책하자면 공원으로. 창을 열면 아무 데도 아닌 자리에서 시작 · 이름표 `친구(가능)` 문턱 · **프롬프트 다이어트** — 189줄 중 51줄이 명령문, 코드가 이미 하는 것만 뺌(장소 안내 25→2줄, 괄호 보기 — 보기를 주면 첫 답에 그대로 나온다) · 고침 — 고른 목소리가 안 불리던 것 · 자는 채로 말하고 놀던 것(`showReply`·`apply*` 한 곳에서 깨움) · 아바타 올리기는 카메라가 아니라 렌즈(`setViewOffset`) |
| **v1.5** | 2026-08-21 | **올린 사이트에서 다이아가 답한다.** 모델 서버가 Vercel 에서 안 닿았다(미국·서울 둘 다 `ConnectTimeout`, 바깥으로 나가는 것은 됨) → **터널**(`python _tunnel.py`, cloudflared, 443 으로 나가서 안 막힘) · **터널 주소는 띄울 때마다 바뀌므로 저장소에 적어 두고 부를 때마다 읽는다** — 배포를 다시 안 해도 따라간다 · **아바타를 같이 올렸다**(28MB, 한도 500MB — 크기는 문제가 아니었다). `/static/` 을 로그인 뒤로 넣어 계정이 있는 사람만 받는다 · 고침 — `ConnectTimeout` 이 `Timeout` 절에 먼저 걸려 **닿지도 못한 것을 '생각이 오래 걸린다'** 고 답하던 것 · `_clean_test_accounts.py`(계정은 전부 한 파일이라 파일이 아니라 그 안의 한 칸을 지워야 한다) · 파일 이름이 한글이면 배포 짐에 안 실린다 |
| **v1.4** | 2026-08-20 | **vercel.com 에 올렸다** (https://diamondai-six.vercel.app) · `store.py` — 적고 읽는 일을 한 곳에 모아 **로컬은 파일, 올린 데서는 Vercel Blob**(private). 나머지는 한 줄도 안 고쳤다 · 환경변수 셋(`SECRET_KEY` 없으면 기계 바뀔 때마다 전원 로그아웃 / `SIGNUP_CODE` 없으면 주소만 알면 누구나 가입 / `VRM_URL`) · `.vercelignore` — Vercel 은 폴더를 통째로 올리므로 `.gitignore` 와 따로 필요하다(토큰 `.env.local` 이 배포 짐에 실릴 뻔했다) · `_verify_server.py` · **아바타는 못 올린다**(`Redistribution_Prohibited`) — `VRM_URL` 로 다른 데를 가리켜야 나온다 |
| **v1.3** | 2026-08-20 | **다이아도 괄호로 상황을 적는다** — `(머리카락을 귀 뒤로 넘긴다) 왜 그렇게 봐?` 처럼 말과 같이도, 상황만도. 예전에는 이름표가 아닌 괄호를 통째로 버려서 몸짓 표에 있는 것만 할 수 있었다 · **괄호 속 문장이 얼굴과 몸이 된다**(`act_reads` 30갈래, 낱말의 앞동강만 봄 — '멋쩍' 하나로 활용형이 다 걸린다. 못 읽는 문장은 글자로만) · TTS 는 괄호를 안 읽는다(지문까지 읽으면 자기 행동을 중계하는 꼴) · 화면에서는 옅고 기울인 글씨 · `_verify_calls.py`(부르는데 없는 함수 찾기) · 고침 — 상황 추천 보기를 눌러도 아무 일도 안 일어나던 것(`sendSituation` 이 지워졌는데 단추가 계속 부르고 있었다) |
| **v1.2** | 2026-08-20 | **로그인**(아이디·비밀번호 — pbkdf2-sha256 + 계정마다 다른 소금 + 20만 번, 비밀번호는 파일에 안 남는다) · **기억이 계정마다 따로**(`memory/u_<아이디>.json`, 보관함도 따로). 회원가입은 빈 기억, 로그인은 그 사람의 기억 — **로그인을 새 기억으로 하면 내 기억도 들어올 때마다 지워진다** · 들여보내기는 `before_request` 한 곳에서 막으므로 새 API 도 저절로 막힌다 · 첫 계정이 계정을 나누기 전의 기억을 물려받는다 · `_verify_login.py`(24가지) · 고침 — `_hash` 의 기본 인자가 정의될 때 묶여 반복 횟수를 올리면 있던 계정이 전부 못 들어오던 것 |
| **v1.1** | 2026-08-20 | **캠 속 사람이 상대**(🧍 — 캠에 비친 곳을 거울로 뒤집어 배경에 깔고, 얼굴 자리로 더미를 옮긴다. 더미를 옮긴 것이라 몸 돌리기·다가오기·닿는 거리가 그대로 따라온다) · 상황 칸과 말 칸을 **엔터 한 번에 함께** (`(다이아를 지긋이 바라본다) 다이아 사랑해`) · 고침 — 상황 칸에 글을 쓰면 스페이스바에 뛰고 shift 에 앉던 것(`isTyping()`) · 가위바위보 손이 사진 단추를 덮던 것(옆으로 펼치게) · 미뤄 둠 — 배경에서 사람 지우기(`/배경지우기`)와 방 짓기(`/방`)는 깨져 보여 뒤로 |
| **v1.0** | 2026-08-19 | **하려던 것을 다 만든 판.** 3인칭 시점(👁 / F — 카메라만 몸에서 떨어져 나가고, **떨어져 나간 시선은 다이아가 모른다**) · 배 부풀리기는 빼고 임신은 자각(프롬프트의 `[아이]`)으로 · 서는 거리를 세 걸음 뒤로 · 입 판정을 판정구 좌표로 · 마이크와 카메라는 아직 실제로 못 써 봤다 |
| **v0.6.1** | 2026-08-19 | 고침 — 입 판정이 입술이 아니라 턱에 가 있던 것(값을 얼굴 표면이 아니라 **판정구 위** 좌표로 다시 잡음, `_fit_mouth.py` 로 남김) · 대화 기록에 자리마다 다른 도구 이름이 안 쓰이던 것(입에 닿은 것을 손가락이라 답했다) · 조사를 받침 따라 |
| **v0.6** | 2026-08-19 | **눈**(사진 보여주기 · 카메라가 다이아의 눈이 됨, 보는 모델과 말하는 모델을 나눔) · **귀**(음성 인식) · **말을 끊고 들어오기** · **시선 추적**(카메라 속 얼굴 쪽으로 고개) · **시간을 안다**(몇 시인지, 며칠 만인지) · 고백을 **친구부터** 받음 · **이력현상을 나가는 쪽에만** (표의 진입선이 이제 진짜 진입선이다 — 저장된 값의 뜻이 달라짐) · 임신 시험 명령어와 배 슬라이더 · `_verify_screen.py`(화면이 끝까지 실행되는지) · 벗긴 옷을 계속 잡으려 하던 것 수정 |
| **v0.5** | 2026-08-19 | **1인칭 상대**(카메라가 곧 눈. 조이스틱·방향키·앉기·점프, 다이아가 보고 따라오고 비켜섬) · **입맞춤**(얼굴을 들이대면 눈을 감고 기다린다) · **아이**(순종 50에서 약속 → 절정 5번 → 배가 불러 옴) · 모델을 `gemma4:12b` 로 (서버 VRAM 이 모자라 31b 가 5.5토큰/초까지 떨어짐, 말투는 6/6 동일) · **TTS 가 61% 지점에서 끊기던 것** 고침 · 잠결 얼굴 · 방을 6m 로 넓힘 · 💤 가 머리를 따라감 · 검사 셋이 옛 기준으로 실패하던 것 고침 · 표정 잠금 다시 뜸(31개) |
| **v0.4** | 2026-08-18 | 호감 눈금 2배 · **고백**(연인이 되어야 광기로) · **기분**(친밀도와 별개) · **순종**(상한 위의 두 번째 저울) · **만화 얼굴 11개** · 몸/옷 겹치기 · 옷 두 번 눌러 벗기 · TTS · 표정 수치 잠금 · 픽셀창 |
| **v0.3** | 2026-08-14 | 만지기(자리 x 도구) · 가위바위보 · 얀데레 단계 · 표현용.vrm 겹치기 · 손가락 관절 · 옷 잡아당기기 · 기억 명령어 · 괄호로 만지기 |
| **v0.2** | 2026-08-14 | `index.html` 이관 완료 · 먼저 말 걸기 · 잠들기 · 돌아다니기 · 말풍선이 얼굴을 따라감 · 속생각 끄기로 3배 빨라짐 |
| **v0.1** | 2026-08-13 | 페르소나와 아바타를 **하나의 개체로 통합**. 감정 정의가 세 곳에 흩어져 있던 것을 `avatar.py` 로 모음. 모델을 `gemma4:31b` 로 교체 |

### 판을 가르는 기준

- **표정은 /face 에서 만든다.** 기본값은 아바타 파일의 그룹 그대로(100)다
- **관계 눈금이 바뀌면** 저장된 값의 뜻이 달라지므로 크게 적는다
- 검사 스크립트가 하나라도 실패하면 판을 내지 않는다
- **화면을 고쳤으면 `_verify_screen.py` 를 꼭 돌린다.** index.html 은
  스크립트가 한 덩어리라, 한가운데서 오류가 나면 파일 끝의
  `loadAvatar()` 까지 못 가서 아바타가 아예 안 나온다.
  화면은 멀쩡히 뜨고 다이아만 없어서 콘솔을 안 열면 원인이 안 보인다
